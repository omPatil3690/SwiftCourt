# QuickCourt Server - Example Environment Variables (Receipt-only mode)

# Core
NODE_ENV=development
PORT=4000

# Database
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/quickcourt?schema=public
# For Neon (example):
# DATABASE_URL="postgresql://<user>:<password>@<neon-host>/<database>?sslmode=require&pgbouncer=true&connect_timeout=15"
# Ensure sslmode=require is present for Neon.

# JWT
ACCESS_TOKEN_SECRET=replace_with_random_64_char_hex
REFRESH_TOKEN_SECRET=replace_with_random_64_char_hex
ACCESS_TOKEN_TTL=15m
REFRESH_TOKEN_TTL=7d

# OTP
OTP_TTL_MINUTES=10

# CORS (for production set to your domain, in dev we allow localhost automatically)
CORS_ORIGIN=http://localhost:8080

# Email (optional)
SMTP_HOST=
SMTP_PORT=
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=QuickCourt <no-reply@quickcourt.local>

# Admin invite secret (required to create ADMIN users via /auth/signup with role=ADMIN)
ADMIN_INVITE_SECRET=replace_with_strong_shared_secret

# Notes:
# 1) Copy this file to `.env` or `.env.local` in the server/ folder and fill values.
# 2) Generate secure secrets for ACCESS_TOKEN_SECRET and REFRESH_TOKEN_SECRET.
# 3) Payments gateway is disabled; the app generates simple receipts.

